---
interface Props {
  title: string;
  author: string;
  description?: string;
  tags?: string[];
}

const { title } = Astro.props;
---

<div class="win7 w-full h-full">
  <div class="window glass active w-full h-full flex flex-col">
    <div class="title-bar">
      <div class="title-bar-text">{title}</div>
      <div class="title-bar-controls">
        <button aria-label="Minimize"></button>
        <button aria-label="Maximize"></button>
        <button aria-label="Close"></button>
      </div>
    </div>
    <div class="window-body flex-1 overflow-hidden">
      <article class="font-sans h-full overflow-auto">
        <!-- Post Content -->
        <div class="content h-full">
          <div class="h-full">
            <slot />
          </div>
        </div>
      </article>
    </div>
  </div>
</div>

<style>
  /* Minimalistic typography */
  .content {
    font-family:
      -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue",
      Arial, sans-serif;
    line-height: 1.6;
    color: var(--color-text);
    padding: 1.5rem;
    background: var(--color-background);
    position: relative;
  }

  .content::after {
    content: "";
    position: absolute;
    bottom: 0;
    right: 0;
    width: 100px;
    height: 100px;
    background: var(--color-accent);
    border-radius: 50% 0 0 0;
    pointer-events: none;
    z-index: 0;
  }

  .content > * {
    position: relative;
    z-index: 1;
  }

  /* Minimalistic headings */
  .content :global(h1) {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text);
    margin: 1.5rem 0 1rem 0;
  }

  .content :global(h2) {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 1.25rem 0 0.75rem 0;
  }

  .content :global(h3) {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 1rem 0 0.5rem 0;
  }

  .content :global(h4) {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 1.25rem 0 0.5rem 0;
  }

  .content :global(h5) {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 1rem 0 0.5rem 0;
  }

  .content :global(h6) {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 1rem 0 0.5rem 0;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Minimalistic paragraph styling */
  .content :global(p) {
    margin: 1rem 0;
    color: var(--color-text);
    font-size: 1rem;
    line-height: 1.6;
  }

  /* Minimalistic text formatting */
  .content :global(strong) {
    font-weight: 700;
  }

  .content :global(em) {
    font-style: italic;
  }

  /* Minimalistic code styling */
  .content :global(code) {
    font-family: "Courier New", monospace;
    background: var(--color-accent);
    padding: 0.2rem 0.4rem;
    border-radius: 3px;
    font-size: 0.9rem;
  }

  /* Minimalistic code blocks */
  .content :global(pre) {
    font-family: "Courier New", monospace;
    background: var(--color-accent);
    padding: 1rem;
    border-radius: 4px;
    margin: 1rem 0;
    overflow-x: auto;
  }

  .content :global(pre code) {
    background: transparent;
    border: none;
    color: inherit;
    padding: 0;
    font-size: 0.9rem;
    line-height: 1.6;
  }

  /* Elegant blockquotes */
  .content :global(blockquote) {
    font-style: italic;
    margin: 2rem 0;
    padding: 1.5rem 2rem;
    background: var(--color-accent);
    border-left: 4px solid var(--color-primary);
    border-radius: 0 8px 8px 0;
    position: relative;
  }

  .content :global(blockquote)::before {
    content: ""
      ";
    font-size: 3rem;
    color: var(--color-primary);
    position: absolute;
    top: -0.5rem;
    left: 1rem;
  }

  .content :global(blockquote)::after {
    content: "
      "";
    font-size: 3rem;
    color: var(--color-primary);
    position: absolute;
    bottom: -1.5rem;
    right: 1rem;
  }

  /* Minimalistic lists */
  .content :global(ul) {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .content :global(ol) {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .content :global(li) {
    margin: 0.25rem 0;
    line-height: 1.6;
  }

  .content :global(ul li) {
    list-style: disc;
  }

  .content :global(ol li) {
    list-style: decimal;
  }

  /* Nested lists */
  .content :global(ul ul),
  .content :global(ol ol),
  .content :global(ul ol),
  .content :global(ol ul) {
    margin: 0.25rem 0;
    padding-left: 1rem;
  }

  /* Beautiful links */
  .content :global(a) {
    color: var(--color-primary);
    text-decoration: none;
    border-bottom: 2px solid transparent;
    transition: all 0.3s ease;
    position: relative;
  }

  .content :global(a:hover) {
    color: var(--color-primary-hover);
    border-bottom-color: var(--color-primary);
  }

  .content :global(a:visited) {
    color: var(--color-primary);
  }

  .content :global(a:active) {
    color: var(--color-primary-hover);
  }

  /* Retro tables */
  .content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 2rem 0;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    background: white;
  }

  .content :global(th) {
    background: var(--color-accent);
    color: var(--color-text);
    padding: 1rem;
    text-align: left;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.9rem;
  }

  .content :global(td) {
    padding: 1rem;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
    vertical-align: top;
  }

  .content :global(tr:nth-child(even)) {
    background: var(--color-accent);
  }

  .content :global(tr:hover) {
    background: var(--color-accent);
    transition: background 0.3s ease;
  }

  /* Enhanced images */
  .content :global(img) {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin: 2rem auto;
    display: block;
    box-shadow:
      0 8px 16px rgba(0, 0, 0, 0.1),
      0 4px 8px rgba(0, 0, 0, 0.06);
    border: 3px solid white;
    transition: all 0.3s ease;
  }

  .content :global(img:hover) {
    transform: scale(1.02) rotate(0.5deg);
    box-shadow:
      0 12px 24px rgba(0, 0, 0, 0.15),
      0 6px 12px rgba(0, 0, 0, 0.1);
  }

  /* Artistic horizontal rules */
  .content :global(hr) {
    border: none;
    height: 1px;
    margin: 3rem 0;
    background: var(--color-border);
  }

  /* Definition lists */
  .content :global(dl) {
    margin: 1.5rem 0;
  }

  .content :global(dt) {
    font-weight: 700;
    color: var(--color-text);
    margin-top: 1rem;
    font-size: 1.1rem;
  }

  .content :global(dd) {
    margin-left: 1.5rem;
    margin-top: 0.5rem;
    color: var(--color-text);
    line-height: 1.7;
  }

  /* Retro keyboard and sample */
  .content :global(kbd) {
    font-family: "Courier New", monospace;
    background: var(--color-accent);
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 0.25rem 0.5rem;
    font-size: 0.8rem;
  }

  .content :global(samp) {
    font-family: "Courier New", monospace;
    background: var(--color-accent);
    color: var(--color-text);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    border: 1px solid var(--color-border);
  }

  /* Highlighted text */
  .content :global(mark) {
    background: var(--color-accent);
    color: var(--color-text);
    padding: 0.1rem 0.3rem;
    border-radius: 3px;
    font-weight: 500;
  }

  .content :global(del) {
    text-decoration: line-through;
    color: #95a5a6;
    text-decoration-color: #e74c3c;
  }

  .content :global(ins) {
    text-decoration: underline;
    color: #27ae60;
    text-decoration-color: #27ae60;
  }

  /* Small text and subscripts */
  .content :global(small) {
    font-size: 0.85rem;
    color: #7f8c8d;
    font-style: italic;
  }

  .content :global(sub) {
    font-size: 0.75rem;
    vertical-align: sub;
    color: #7f8c8d;
  }

  .content :global(sup) {
    font-size: 0.75rem;
    vertical-align: super;
    color: #7f8c8d;
  }

  /* Enhanced focus states */
  .content :global(a:focus),
  .content :global(button:focus),
  .content :global([role="button"]:focus) {
    outline: 3px solid #3498db;
    outline-offset: 2px;
    border-radius: 4px;
  }

  /* Responsive design */
  @media (max-width: 768px) {
    .content {
      padding: 1rem;
      font-size: 0.95rem;
    }

    .content :global(h1) {
      font-size: 2rem;
    }

    .content :global(h2) {
      font-size: 1.75rem;
    }

    .content :global(h3) {
      font-size: 1.4rem;
    }

    .content :global(pre) {
      padding: 1rem;
      font-size: 0.8rem;
    }

    .content :global(blockquote) {
      padding: 1rem 1.5rem;
    }
  }

  /* Print styles */
  @media print {
    .content {
      background: white;
      box-shadow: none;
    }

    .content :global(pre) {
      background: #f8f9fa;
      color: #2c3e50;
      border: 1px solid #dee2e6;
    }

    .content :global(code) {
      background: #f8f9fa;
      color: #2c3e50;
      border: 1px solid #dee2e6;
    }
  }
</style>
